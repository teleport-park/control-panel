import { ExtendedFiltersDirective } from './extended-filters.directive';

describe('ExtendedFiltersDirective', () => {
  it('should create an instance', () => {
    const directive = new ExtendedFiltersDirective();
    expect(directive).toBeTruthy();
  });
});
