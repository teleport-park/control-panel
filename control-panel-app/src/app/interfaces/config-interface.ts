export interface Config {
  users: {
    sortedColumns: string[];
  };
  staff: {
    sortedColumns: string[];
  };
  groups: {
    sortedColumns: string[];
  };
  permissions: {
    sortedColumns: string[];
  };
}
