/**
 * interface for build extended filters url params
 */
export interface ExtendedFilterUrlParamsInterface {
  /**
   * get extended filters url params
   * @param params
   */
  getExtendedFilterParams(params: any): string;
}
